# FastAPI backend dependencies
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
websockets>=12.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6

# Database and data processing (уже должны быть установлены)
pandas>=2.1.0
openpyxl>=3.1.2

# Optional: для улучшенной производительности
orjson>=3.9.0  # Более быстрая JSON сериализация

# Rate limiting
slowapi>=0.1.9

# Caching (опционально, для улучшения производительности)
redis>=5.0.0
aioredis>=2.0.0
hiredis>=2.2.0  # Более быстрый парсер для Redis (опционально)

# Metrics (опционально, для мониторинга)
prometheus-client>=0.19.0

# Code quality (linting, formatting, type checking)
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.5.0
pylint>=3.0.0

# Error tracking (опционально)
sentry-sdk[fastapi]>=1.38.0

# System monitoring (опционально, для health checks)
psutil>=5.9.0

# Notifications (опционально)
requests>=2.31.0  # Для Telegram уведомлений

# PDF reports (опционально)
reportlab>=4.0.0  # Для генерации PDF отчетов

# Google Sheets API (для импорта данных)
gspread>=5.12.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
